#!/usr/bin/env ruby

require 'trck'

command = ARGV.shift

case command
when 'reset'
  Storage.load.reset
when 'setup'
  Trck.setup(ARGV)

when 'status'

when 'start'

when 'stop'

when 'project'

when 'org'
  puts Trck.org(ARGV)
when 'sync'

end
